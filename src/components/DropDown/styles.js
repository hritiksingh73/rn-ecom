import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    container: {
      flexDirection: 'row',
    },
    dropdown: {
      borderColor: 'white',
      marginLeft: 80,
      height: 18,
      width: 200,
    },
  });

  export default styles;