import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    marginLeft: 120,
    width:150
  },
    dropdown: {
      borderColor: 'white',
      alignItems: 'center',
      height: 18,
      width: 120,
      margin: 10,
      
    },
  });

  export default styles;