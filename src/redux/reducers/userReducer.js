import { ADD_TODO } from "../action";

const initialState = {
  userName: "something"
};


const userReducer = (state = initialState, action) => {
  switch (action.type) {
    // case ADD_TODO:

    // 
    default:
      return state
  }
}

export default userReducer